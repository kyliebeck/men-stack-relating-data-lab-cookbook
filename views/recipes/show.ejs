<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>
        <%=recipe.name %>

    </title>
</head>

<body>
    <h1>
        <%=recipe.name%>
    </h1>
    <a href="/">Back Home
    </a>


    <% if (recipe.instructions) { %>
        <p>Instructions: <%=recipe.instructions%>
        </p>
        <% } else { %>
            <p>Instructions have not been included in this recipe.</p>
            <%}%>
                <% if (recipe.ingredients) { %>
                    <p>Ingredients: <%=recipe.ingredients.map((ingredient)=> {
                            return ingredient.name
                            }).join(", ")%>

                    </p>
                    <% } else { %>
                        <p>Ingredients have not been added to this recipe.</p>
                        <% } %>

                            <form class=" delete-form" action="/recipes/<%=recipe._id%>?_method=DELETE" method="POST">
                                <button class="delete-button" type="submit">Delete <%= recipe.name %></button>
                            </form>
                            <form class="edit-form" action="/recipes/<%= recipe._id %>/edit">
                                <button class="edit-text">Edit <%= recipe.name %>
                            </form>
</body>